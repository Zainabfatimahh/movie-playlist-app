# Implementation Overview & Architecture Diagram

**Created**: February 5, 2026  
**Status**: ✅ Complete and Production Ready

---

## 🏗️ System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER (Frontend)                   │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌────────┐  │
│  │ Signin  │  │ Signup  │  │Playlist │  │   Add   │  │  Edit  │  │
│  │ Page    │  │  Page   │  │  Page   │  │  Page   │  │  Page  │  │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  └───┬────┘  │
│       │            │            │            │            │        │
│       └────────────┼────────────┼────────────┼────────────┘        │
│                    │            │            │                     │
│              HTTP/REST with JWT/Cookies      │                     │
│                    │            │            │                     │
└────────────────────┼────────────┼────────────┼─────────────────────┘
                     │            │            │
                     ▼            ▼            ▼
┌──────────────────────────────────────────────────────────────────┐
│                    API GATEWAY / FASTIFY (Port 3001)              │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ Middleware: CORS, Helmet, Auth, Logging                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────┐  ┌──────────────────────────────────┐  │
│  │   AUTH ROUTES        │  │    MOVIE ROUTES                  │  │
│  ├──────────────────────┤  ├──────────────────────────────────┤  │
│  │ POST   /auth/signup  │  │ GET    /movies                   │  │
│  │ POST   /auth/login   │  │ GET    /movies/:id               │  │
│  │ POST   /auth/logout  │  │ POST   /movies                   │  │
│  │ GET    /me           │  │ PUT    /movies/:id               │  │
│  └──────┬───────────────┘  │ DELETE /movies/:id               │  │
│         │                  └──────────┬─────────────────────────┘  │
│  ┌──────▼─────────────────┐      ┌────▼────────────────────────┐  │
│  │ AuthService           │      │ MovieService                │  │
│  │ ├─ signup()           │      │ ├─ createMovie()            │  │
│  │ ├─ login()            │      │ ├─ getMovies()              │  │
│  │ ├─ logout()           │      │ ├─ getMovieById()           │  │
│  │ └─ createSession()    │      │ ├─ updateMovie()            │  │
│  └──────┬─────────────────┘      │ └─ deleteMovie()            │  │
│         │                        └────┬──────────────────────────┘  │
│         └────────────┬─────────────────┘                          │
│                      │                                             │
│                      ▼ (Prisma ORM)                               │
└──────────────────────────────────────────────────────────────────┘
                       │
                       │ TCP/IP
                       │
┌──────────────────────▼──────────────────────────────────────────┐
│            DATABASE LAYER (PostgreSQL, Port 5432)                │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  TABLE: User                                               │  │
│  │  ├─ id (UUID, PK)                                          │  │
│  │  ├─ name (VARCHAR)                                         │  │
│  │  ├─ email (VARCHAR, UNIQUE)                                │  │
│  │  ├─ password (VARCHAR, hashed)                             │  │
│  │  ├─ role (VARCHAR)                                         │  │
│  │  ├─ createdAt (TIMESTAMP)                                  │  │
│  │  └─ updatedAt (TIMESTAMP)                                  │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  TABLE: Movie                                              │  │
│  │  ├─ id (INT, PK)                                           │  │
│  │  ├─ title (VARCHAR)                                        │  │
│  │  ├─ year (VARCHAR)                                         │  │
│  │  ├─ imageUrl (VARCHAR)                                     │  │
│  │  ├─ ownerId (UUID, FK → User.id)                           │  │
│  │  ├─ createdAt (TIMESTAMP)                                  │  │
│  │  └─ updatedAt (TIMESTAMP)                                  │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  TABLE: Session                                            │  │
│  │  ├─ id (UUID, PK)                                          │  │
│  │  ├─ userId (UUID, FK → User.id)                            │  │
│  │  ├─ refreshToken (VARCHAR)                                 │  │
│  │  ├─ expiresAt (TIMESTAMP)                                  │  │
│  │  ├─ createdAt (TIMESTAMP)                                  │  │
│  │  └─ updatedAt (TIMESTAMP)                                  │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Relationships:                                                   │
│  • User (1) ──────── (N) Movie     [ownerId FK]                  │
│  • User (1) ──────── (N) Session   [userId FK]                   │
│  • Cascade Delete: User → Movies, Sessions                       │
│                                                                   │
│  Indexes:                                                         │
│  • User.email (UNIQUE)                                            │
│  • Movie.ownerId (for fast user queries)                         │
│  • Movie.createdAt (for sorting)                                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT                                   │
└──────────┬──────────────────────────────────────────┬────────────┘
           │                                          │
    1. User enters email + password                   │
           │                                          │
           ▼                                          │
┌─────────────────────────────────────────────────────────────────┐
│            POST /auth/signup OR /auth/login                      │
│            + Zod Validation + Argon2 Hash Check                  │
└──────────┬──────────────────────────────────────────┬────────────┘
           │                                          │
    2. Server validates & creates tokens            │
           │                                          │
           ├─ AccessToken (JWT, 15 min)              │
           ├─ RefreshToken (Cookie, 7 days)          │
           └─ User object                            │
           │                                          │
    3. Browser stores locally                        │
           │                                          │
           ├─ localStorage: accessToken               │
           └─ Cookies: refreshToken (HttpOnly)        │
           │                                          │
    4. Subsequent requests include:                  │
           │                                          │
           ├─ Authorization: Bearer <accessToken>     │
           └─ Cookies: <refreshToken> (automatic)     │
           │                                          │
           ▼                                          │
┌─────────────────────────────────────────────────────────────────┐
│            authMiddleware verifies token                          │
│            Extracts userId and attaches to request                │
└──────────┬──────────────────────────────────────────┬────────────┘
           │                                          │
    5. Route handler accesses request.userId         │
           │                                          │
           ▼                                          │
    ✅ Request processed with user context           │
                                                      │
    ❌ 401 Unauthorized if token missing/invalid    │
           │                                          │
           ▼                                          │
    5. On logout: POST /auth/logout                  │
           • Clears refresh token cookie              │
           • Invalidates session                      │
           • Client clears localStorage               │
                                                      │
    ✅ User logged out securely                      │
```

---

## 📊 Data Flow - Create Movie Example

```
┌──────────────────────────────────────────────────────────────┐
│                    Frontend: app/add/page.tsx                 │
│  User fills: title="The Matrix", year="1999", imageUrl="..." │
│  Clicks "Save"                                               │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  POST /movies                                                │
│  Headers:                                                    │
│    Content-Type: application/json                            │
│    Authorization: Bearer <accessToken>                       │
│  Body: {                                                     │
│    "title": "The Matrix",                                    │
│    "year": "1999",                                           │
│    "imageUrl": "https://..."                                 │
│  }                                                           │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  Backend: routes/movies.ts → POST /movies handler            │
│  ├─ Extract userId from request.userId                       │
│  ├─ Validate body with CreateMovieSchema (Zod)               │
│  └─ Call MovieService.createMovie(userId, data)              │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  MovieService.createMovie()                                  │
│  ├─ Validate title is not empty                              │
│  ├─ Validate year is YYYY format (if provided)               │
│  └─ prisma.movie.create({                                    │
│       title, year, imageUrl,                                 │
│       ownerId: userId                                        │
│     })                                                        │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼ (Prisma ORM)
┌──────────────────────────────────────────────────────────────┐
│  PostgreSQL INSERT                                           │
│  INSERT INTO "Movie" (                                       │
│    title, year, imageUrl, "ownerId",                         │
│    "createdAt", "updatedAt"                                  │
│  ) VALUES (                                                  │
│    'The Matrix', '1999', 'https://...',                      │
│    'uuid-123', NOW(), NOW()                                  │
│  )                                                           │
│  RETURNING *;                                                │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  Return Movie object                                         │
│  {                                                           │
│    "id": 1,                                                  │
│    "title": "The Matrix",                                    │
│    "year": "1999",                                           │
│    "imageUrl": "https://...",                                │
│    "ownerId": "uuid-123",                                    │
│    "createdAt": "2024-01-01T00:00:00Z",                      │
│    "updatedAt": "2024-01-01T00:00:00Z"                       │
│  }                                                           │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼ (HTTP 201 Created)
┌──────────────────────────────────────────────────────────────┐
│                    Frontend: Update state                     │
│  Add movie to state.movies array                             │
│  Show success toast                                          │
│  Redirect to /playlist to view all movies                    │
│  ✅ Movie created and visible in list                        │
└──────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
Development (Your Machine)
├── Frontend: npm run dev (http://localhost:3000)
├── Backend: npm run dev (http://localhost:3001)
└── Database: docker-compose up postgres

     ▼ Deploy to Production ▼

Production (Cloud Server)
┌─────────────────────────────────────────────────────┐
│                Vercel / AWS / Heroku                │
├─────────────────────────────────────────────────────┤
│  Frontend (Next.js)                                 │
│  ├─ Static assets served from CDN                   │
│  └─ API calls to: https://api.yourdomain.com       │
├─────────────────────────────────────────────────────┤
│  Backend (Fastify) - Serverless or Container       │
│  ├─ Environment: NODE_ENV=production                │
│  ├─ Secrets: JWT keys, database URL                 │
│  └─ Healthcheck: GET /health                        │
├─────────────────────────────────────────────────────┤
│  Database (AWS RDS PostgreSQL)                      │
│  ├─ Multi-AZ deployment                             │
│  ├─ Automated backups                               │
│  ├─ SSL/TLS encryption                              │
│  └─ Monitoring & alerts                             │
└─────────────────────────────────────────────────────┘
     │
     ├─ CDN: CloudFront (images)
     ├─ Email: SendGrid (notifications)
     ├─ Monitoring: Sentry (errors)
     ├─ Analytics: Datadog (logs)
     └─ Backups: S3 (database snapshots)
```

---

## 📈 Request/Response Cycle

```
1. Request
   ┌─────────────────────────────────────────────────┐
   │ GET /movies?page=1&limit=20                      │
   │ Authorization: Bearer eyJhbGc...                │
   │ Accept: application/json                         │
   └─────────────────────────────────────────────────┘
                        │
                        ▼
2. Server Processing
   ┌─────────────────────────────────────────────────┐
   │ ├─ fastifyCors (check origin)                   │
   │ ├─ fastifyHelmet (add security headers)         │
   │ ├─ authMiddleware (verify token)                │
   │ ├─ Route handler (validate query params)        │
   │ ├─ MovieService (fetch from DB with Prisma)    │
   │ ├─ Logging (request details)                    │
   │ └─ Error handling (if any)                      │
   └─────────────────────────────────────────────────┘
                        │
                        ▼
3. Response
   ┌─────────────────────────────────────────────────┐
   │ HTTP/1.1 200 OK                                 │
   │ Content-Type: application/json                  │
   │ X-Content-Type-Options: nosniff                 │
   │ X-Frame-Options: DENY                           │
   │                                                  │
   │ {                                               │
   │   "items": [                                    │
   │     {                                           │
   │       "id": 1,                                  │
   │       "title": "The Matrix",                    │
   │       "year": "1999",                           │
   │       "imageUrl": "https://...",                │
   │       "ownerId": "uuid",                        │
   │       "createdAt": "2024-01-01T00:00:00Z",      │
   │       "updatedAt": "2024-01-01T00:00:00Z"       │
   │     }                                           │
   │   ],                                            │
   │   "meta": {                                     │
   │     "page": 1,                                  │
   │     "limit": 20,                                │
   │     "total": 42,                                │
   │     "totalPages": 3                             │
   │   }                                             │
   │ }                                               │
   └─────────────────────────────────────────────────┘
```

---

## 🔄 Error Handling Flow

```
User Request
      │
      ▼
┌──────────────────┐
│ Input Validation │  (Zod Schema)
└────────┬─────────┘
         │
    ✓ Valid │  ✗ Invalid
         │      │
         ▼      ▼
       ┌─────────────────────────────────┐
       │  Return 400 Bad Request         │
       │  {                              │
       │    "error": {                   │
       │      "code": "VALIDATION_ERROR",│
       │      "details": [...]           │
       │    }                            │
       │  }                              │
       └─────────────────────────────────┘
       │
       ▼
┌──────────────────┐
│ Authentication   │  (JWT Middleware)
└────────┬─────────┘
         │
    ✓ Valid │  ✗ Missing/Invalid
         │      │
         ▼      ▼
       ┌─────────────────────────────────┐
       │  Return 401 Unauthorized        │
       │  {                              │
       │    "error": {                   │
       │      "code": "UNAUTHORIZED"     │
       │    }                            │
       │  }                              │
       └─────────────────────────────────┘
       │
       ▼
┌──────────────────┐
│ Authorization    │  (Owner Check)
└────────┬─────────┘
         │
    ✓ Owner │  ✗ Not Owner
         │      │
         ▼      ▼
       ┌─────────────────────────────────┐
       │  Return 403 Forbidden           │
       │  {                              │
       │    "error": {                   │
       │      "code": "FORBIDDEN"        │
       │    }                            │
       │  }                              │
       └─────────────────────────────────┘
       │
       ▼
┌──────────────────┐
│ Database Query   │  (Prisma)
└────────┬─────────┘
         │
    ✓ OK   │  ✗ Not Found
         │     │
         ▼     ▼
       ┌──────────────────────────────┐
       │ Return 404 Not Found         │
       │ {                            │
       │   "error": {                 │
       │     "code": "NOT_FOUND"      │
       │   }                          │
       │ }                            │
       └──────────────────────────────┘
       │
       ▼
┌──────────────────┐
│ Process Request  │
└────────┬─────────┘
         │
    ✓ OK   │  ✗ Error
         │      │
         ▼      ▼
       ┌──────────────────────────────┐
       │ Return 500 Server Error      │
       │ {                            │
       │   "error": {                 │
       │     "code": "INTERNAL_ERROR" │
       │   }                          │
       │ }                            │
       └──────────────────────────────┘
       │
       ▼
┌──────────────────┐
│ Return Success   │  (200/201/204)
└──────────────────┘
       │
       ▼ (logged to Pino)
   User receives response
```

---

## 🎯 Component Interaction

```
┌────────────┐
│  Frontend  │
│  (Next.js) │
└─────┬──────┘
      │ HTTP
      │
┌─────▼──────────────────────────────────────────┐
│              Fastify Server                     │
├────────────────────────────────────────────────┤
│  Middleware Layer                              │
│  ├─ CORS Check                                 │
│  ├─ Helmet Headers                             │
│  ├─ Auth Verification (if protected route)     │
│  └─ Logging                                    │
├────────────────────────────────────────────────┤
│  Routes Layer                                  │
│  ├─ /auth/* routes                             │
│  │  └─ Call AuthService                        │
│  └─ /movies/* routes                           │
│     └─ Call MovieService (with user ID)        │
├────────────────────────────────────────────────┤
│  Service Layer                                 │
│  ├─ AuthService                                │
│  │  ├─ Hash/verify passwords (Argon2)          │
│  │  ├─ Generate tokens (JWT)                   │
│  │  └─ Manage sessions                         │
│  └─ MovieService                               │
│     ├─ Validate input                          │
│     ├─ Check authorization                     │
│     └─ Call Prisma client                      │
├────────────────────────────────────────────────┤
│  Data Layer (Prisma ORM)                       │
│  ├─ Build queries                              │
│  ├─ Type-safe database access                  │
│  └─ Handle relationships                       │
└─────┬──────────────────────────────────────────┘
      │
      ▼
┌────────────────┐
│  PostgreSQL    │
│  Database      │
└────────────────┘
```

---

## 📊 HTTP Status Codes Used

```
✅ Success (2xx)
├── 200 OK ..................... GET, login, get movie
├── 201 Created ................ POST signup, create movie
└── 204 No Content ............. POST logout, DELETE movie

⚠️ Client Error (4xx)
├── 400 Bad Request ............ Invalid input/validation
├── 401 Unauthorized ........... Missing/invalid token
├── 403 Forbidden .............. Not movie owner
└── 404 Not Found .............. Movie doesn't exist

❌ Server Error (5xx)
└── 500 Internal Server Error .. Database error, etc
```

---

**Architecture designed for scalability, security, and maintainability** ✅

*Last Updated: February 5, 2026*
